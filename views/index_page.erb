<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Movies</title>
    <link rel="stylesheet" href="home.css" />
  </head>

  <body>
    <h1>Movies</h1>

    <ul>

    <% @sorted_movies.each do |movie| %>
      <li><a href="/movies/<%= movie[:id] %>">
      <%= movie[:title] %></a></li><% end %>


    </ul>
    <% @pages.each do |page| %>
    <a href="/movies?page=<%= page %>">
    <%= page %></a>
    <% end %>
  </body>
</html>
